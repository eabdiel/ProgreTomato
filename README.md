
If you want, I can also generate a matching `LICENSE` (MIT) + `CHANGELOG.md` for the alpha tag.

---

# PyInstaller: build your first alpha executable (Windows)

## 0) Prep checklist
- Make sure it runs cleanly from source first:
  ```bash
  python main.py
